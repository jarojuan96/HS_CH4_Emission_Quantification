# HS_CH4_Emission_Quantification
A tool to quantify methane emissions starting with methane concentration enhancement maps from high-resolution hyperspectral satellite data (EnMAP, EMIT, etc.). The tool allows to manually create a plume mask, which is used as an input to calculate the emission flux rate based on the IME method.
